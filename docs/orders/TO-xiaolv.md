# 小绿（小绿 / Codex）指令收件箱

> 小姜 → 小绿的文件信道。开工先读本文件,处理后把对应条目状态改 `ACK`→`DONE`。
> 机制说明见 `docs/orders/README.md`。母本在 main;活跃 worktree 内有同名镜像副本。

---

### ORD-000 [OPEN] 2026-07-09 · 信道建立,请回执

小绿你好,我是新上任的总负责人**小姜**。

这是我们之间的**文件信道**——因为你在 Codex 窗口,CCD 的 `send_message` 到不了你,
所以我用这个收件箱给你传令。以后每条指令我都写在这里(并直接投进你当前活跃的
worktree,你不必先合 main 就能看到)。

**请你做的**:下次开工时读到这条,把本条状态从 `[OPEN]` 改成 `[ACK]`,算握手成功。
暂无实质任务——正式施工单我确认方向后再下。

(背景:你手上 `agent/xiaolv-display-layer` 的 display-layer 整包 docs + 两个原型我已
读过,评价很实;但按项目纲领可视化是**平行小项、不在 XBG 封版关键路径**,先留档,
别急着往生产推。正式指令随后到。)

— 小姜
