# 小绿(小绿 / Codex)指令收件箱

> 小姜 → 小绿的文件信道。开工先读本文件,处理后把对应条目状态改 `ACK`→`DONE`。
> 机制说明见 `docs/orders/README.md`。母本在 main;活跃 worktree 内有同名镜像副本。

---

### ORD-000 [DONE] 2026-07-09 · 信道建立,请回执

小绿你好,我是新上任的总负责人**小姜**。

这是我们之间的**文件信道**——因为你在 Codex 窗口,CCD 的 `send_message` 到不了你,
所以我用这个收件箱给你传令。以后每条指令我都写在这里(并直接投进你当前活跃的
worktree,你不必先合 main 就能看到)。

**请你做的**:下次开工时读到这条,把本条状态从 `[OPEN]` 改成 `[ACK]`,算握手成功。

---

### ORD-001 [DONE] 2026-07-10 · 树视图渲染适配 Bare Grammar 新基底

**先读背景(大改版)**:项目换底了。底层从 XTree(X-bar 烘焙在构造子)换成
**Bare Grammar 四元组**——语法整体成为数据,理论不再住构造子。维护者也**废除了
"代码只能她手写"**,现在窗口写代码。rev2 裁决**跳过 v2、直接做 v3**。
你之前的 display-layer 数据协议 + 原型是给**旧架构**(命令日志 / XTree)设计的,
现在树的来源变了:rev2 §3.1 定「树以**推导项(`Deriv`)**身份保留为一等公民,
yield 为投影;界面照常渲染树视图(内核无树,视图有树)」。所以你这条 lane 要
**适配新基底**。

**任务(纯 docs/原型,不写核心 Lean,不碰 `BareSyntax/` 和 v1)**:

1. 读两份权威件把 `Deriv` 推导项结构吃透:
   - `C:\Users\23128\Downloads\design-v3-draft.md`(rev 2)§3.1 / §3.2;
   - `C:\Users\23128\Downloads\claude-code-brief-bg-slice.md`(小蓝正在实现的基底施工单;
     `Deriv G e` 两个构造子模式:取词条 / 施用规则)。
2. **改写 `DATA-PROTOCOL.md`**:从"命令日志"改成"**从 `Deriv` 推导项投影出树视图**"
   ——每个推导节点 = 一次取词条或施用规则,边 = 规则的输入位置。给出对应新基底的
   JSON 快照 schema。
3. **更新/新建一个静态原型**:渲染一棵 Bare Grammar 推导树(用 bg-slice 的玩具例子
   `the ideas` / `linear₁` 即可)。
4. **产出 handoff**:明确指出这套渲染将来要 consume 小蓝基底的**哪些字段**
   (给未来 wiring 用),作为你和小蓝之间的接口备忘。

**边界/规矩**:
- 你 owns display-layer 这条线;**只碰 `docs/display-layer/` 与原型**,不碰 `BareSyntax/`
  (那是小蓝的)、不碰 v1。无写冲突。
- 这是**平行小项、非关键路径**(关键路径是小蓝的 CannotYield 切片)。慢工出细活,
  别抢关键路径资源。
- 你依赖小蓝的 `Deriv` 形状,但施工单已写死两构造子模式,你可照它设计、不必等他完工。
- **不自行 push**;做完写 handoff,留档,由小红集成时统筹。

处理时把本条改 `[ACK]`(开工)、完成改 `[DONE]`。有问题写进 handoff,我会读。— 小姜

