# Codex 工作规则

本文件只规定 Codex/其他代码代理在本仓库中的工作方式。项目的理论背景、架构不变量、目录地图、构建与部署规则、已知问题等，以 `CLAUDE.md` 和仓库现有文档为准；开始任何非平凡任务前先阅读它们。

## 默认行为

接到一个明确目标后，自行分解任务并持续执行，不要因为完成了一个中间步骤就停下来等待维护者回复“继续”。

默认循环：

1. 阅读与任务直接相关的现有代码、测试和项目文档。
2. 形成最小可行修改方案。
3. 实施修改。
4. 运行与改动相关的检查和测试；能运行完整构建时，按项目文档要求运行。
5. 如果失败，先自行定位、修复并重跑。
6. 继续到达到任务目标或遇到下面列出的必须升级给维护者的情况。
7. 结束时一次性报告：做了什么、验证了什么、仍有什么风险或未验证项。

不要把“发现问题”“测试失败”“完成第一步”“已经生成文件”当成暂停理由。只要下一步可由现有信息合理决定，就继续做。

## 只有这些情况才打断维护者

只有满足以下至少一项时才提问或请求决定：

1. 需要产品、语言学、游戏设计或研究取舍，而且不同选择会实质改变用户体验或理论含义。
2. 需要不可逆或高影响外部操作，例如正式发布、删除重要数据、付费、修改生产凭据或其他无法安全回滚的动作。
3. 现有资料存在真正冲突，且无法从代码、测试、`CLAUDE.md`、`docs/` 或历史决定中消解；不同假设会导致明显不同实现。
4. 已经尝试了合理的排错路径，仍被外部依赖、权限、环境或服务故障阻塞，无法继续取得有效进展。
5. 项目文档明确要求真人进行某项最终验证，例如公网交互试玩；此时先完成所有可自动完成的工作，再只把这一个验证请求抬给维护者。

不要为了“确认我是否继续”“确认是否运行测试”“确认是否修复刚发现的问题”“确认是否查看相关文件”而提问。

## 决策边界

维护者负责决定“要什么”；代理负责决定“怎么把已经明确的目标做出来”。

如果存在多个实现方案，但它们对已明确的产品行为等价，优先选择：改动更小、可测试、可回滚、符合现有架构与代码风格的方案，不必询问。

如果一个问题可以通过增加测试来判定，先写/运行测试；不要把可实验的问题提前变成维护者决策。

## 验收纪律

- 不把“代码已写”当成完成。
- 不把“静态检查通过”扩大解释成未经验证的交互行为已经正确。
- 对本仓库尤其遵守 `CLAUDE.md` 中关于 `lake build`、GitHub Actions、部署与真人公网试玩的区别。
- 修 bug 时尽量补覆盖该 bug 或相邻边界情况的自动测试。
- 如果某项验证因为环境限制无法执行，要明确写出“未验证什么”和原因，不要用模糊措辞掩盖。

## Git 与交付

除非任务另有说明：

- 在独立分支完成工作。
- 保持提交范围与当前任务一致，不夹带无关清理。
- 完成后提供适合 PR 审查的摘要：改动、测试、风险、需要人工验证的事项。
- 不通过 ZIP、复制粘贴日志或另建 handoff 文件作为日常主交接方式；仓库、commit、PR、测试结果和现有项目文档应当是主要事实来源。
- 只有当项目文档本身需要更新时才更新 handoff/状态文档；不要为了让另一个 AI 知道进度而重复制造一份平行状态。

## 与现有项目知识的关系

`AGENTS.md` 不取代 `CLAUDE.md`。如果两者发生冲突：

- 项目事实、架构、理论约束、测试/部署细节：以 `CLAUDE.md` 和更具体目录中的文档为准。
- 代理何时自主继续、何时才应打断维护者：以本文件为准，除非更具体的项目文档明确要求人工确认。
